# HTTP & async
aiohttp>=3.9.0
requests>=2.31.0

# Async SQLite
aiosqlite>=0.20.0

# HTML parsing
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Compression (aiohttp optional dep, avoids warnings)
zstandard>=0.22.0
